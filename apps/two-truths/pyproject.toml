[project]
name = "two-truths-and-one-lie"
version = "0.1.0"
description = "Wizard Games — Two Truths & A Lie (WebSocket multiplayer)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "jinja2>=3.1.0",
    "qrcode[pil]>=7.4",
    "python-multipart>=0.0.9",
    "websockets>=12.0",
]

[project.scripts]
start = "main:run_server"
